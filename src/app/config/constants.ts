// src/app/config/constants.ts

/**
 * 애플리케이션 전역에서 사용하는 기본 상수 모음.
 * 시선 추적 프레임레이트, 드웰(응시) 판정 시간, 시선 좌표 스무딩 윈도우 크기를 정의한다.
 */
export const APP_CONSTANTS = {
  DEFAULT_FPS: 30,

  DEFAULT_DWELL_TIME: 1500,

  DEFAULT_SMOOTHING_WINDOW: 5,
}
