// src/app/routes/Router.tsx

import AppShell from '@/app/AppShell'

/**
 * 앱 쉘(AppShell)을 렌더링하는 최상위 라우터 컴포넌트.
 * @returns AppShell 렌더 결과
 */
export default function Router() {
  return <AppShell />
}
