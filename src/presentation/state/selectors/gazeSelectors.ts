// // selectors/gazeSelectors.ts

// import type { GazeState } from '@presentation/state/gazeStore'

// export const selectCursorPosition = (
//   state: GazeState
// ) => ({
//   x: state.filteredGaze?.x ?? 0,
//   y: state.filteredGaze?.y ?? 0,
// })

// export const selectTrackingStatus = (
//   state: GazeState
// ) => ({
//   isTracking: state.isTracking,
//   fps: state.fps,
// })

// export const selectDebugInfo = (
//   state: GazeState
// ) => ({
//   raw: state.rawGaze,
//   filtered: state.filteredGaze,
//   fps: state.fps,
//   confidence: state.confidence,
// })
